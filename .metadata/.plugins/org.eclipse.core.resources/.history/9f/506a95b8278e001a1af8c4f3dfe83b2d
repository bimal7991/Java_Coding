package codechef;

import java.util.*;

public class CHANDF {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		int T = sc.nextInt();
		while (T-- > 0) {
			long x = sc.nextLong(), y = sc.nextLong(), l = sc.nextLong(), r = sc.nextLong();
			long max = 0;
			long ans=x|y;
			
			if(x==0 || y==0)
				System.out.println(0);
			else if(r>=ans && l<=ans)
			{
			   System.out.println(ans);
			}
			else if((l|ans)<r)
			{
				System.out.println((l|ans));
			}
			else if ((ans&l)==ans || (x&y)==0)
			{
				System.out.println(l);
			}
			else if(l>ans)
			{
			    	int bitx=Long.toBinaryString(x).length();
		    		int bity=Long.toBinaryString(y).length();
		    		String btr=Long.toBinaryString(ans);
		    		int minbit=Math.min(bitx, bity);
			    
			    		
			    		String str=btr.substring(btr.length()-minbit,btr.length());
			    		//System.out.println(str);
			    		long val=Long.parseLong(str,2);
			    		long ans2=val|l;
			    		System.out.println(ans2);	
			    	
			    	
			    	//System.out.println(btr);
			    	
			    	
			 }
			else if(r<ans){
				
				
				int bitx=Long.toBinaryString(x).length();
	    		int bity=Long.toBinaryString(y).length();
	    		String btr=Long.toBinaryString(ans);
	    		//System.out.println(btr);
	    		int minbit=Math.min(bitx, bity);
		    
		    		
		    		String str=btr.substring(btr.length()-minbit,btr.length());
		    		//System.out.println(str);
		    		long val=Long.parseLong(str,2);
		    		long ans2=val&l;
		    		 max=(x&ans2)*(y&ans2);
		    		 long ind=ans2;
		    		for(long i=ans2+1;i<=r;i++)
		    		{
		    			long value=(x&i)*(y&i);
		    			if(value>max)
		    			{
		    				max=value;
		    				ind=i;
		    			}
		    		}
		    		
		    		
		    		System.out.println(ind);	
				
			     }
			
			
			     
			}
			 

		}
	}



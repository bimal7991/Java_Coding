package codechef;
import java.util.*;
public class STRNO {
   public static void main(String[] args) {
	Scanner sc=new Scanner(System.in);
	
	
	
	
	
	int T=sc.nextInt();
	while(T-->0)
	{
	  long X=sc.nextLong();
	  long K=sc.nextLong();
//	  if(Math.pow(2, K)<=X)
//	  {
//		  System.out.println(1);
//	  }
//	  else
//	  {
//		  System.out.println(0);
//	  }
	  int flag=0;
	  for(int i=1;i<=1000000;i++)
	  {
		  int divisor=printDivisors(i);
		  int prime=getprime(i);
		  System.out.println("divisor="+divisor+"prime"+prime);
		  if(divisor==X && prime ==K)
		  {
			  flag=1;
			  break;
			  
		  }
		  
	  }
	  if(flag==1)
		  System.out.println(1);
	  else
		  System.out.println(0);
	  
	  
	  
	  
	}
 }
   static int printDivisors(int n) 
   { 
       // Vector to store half of the divisors 
       Vector<Integer> v = new Vector<>(); 
       for (int i=1; i<=Math.sqrt(n); i++) 
       { 
           if (n%i==0) 
           { 
               if (n/i == i) // check if divisors are equal 
                  v.add(i);
               else
               { 
                    v.add(i);
      
                   // push the second divisor in the vector 
                   v.add(n/i); 
               } 
           } 
       } 
      
       // The vector will be printed in reverse 
      
//       for (int i=v.size()-1; i>=0; i--) 
//            System.out.printf("%d ", v.get(i)); 
       return v.size();
   } 
   static int getprime(int n) 
   { 
       // Vector to store half of the divisors 
       Vector<Integer> v = new Vector<>(); 
       for (int i=1; i<=Math.sqrt(n); i++) 
       { 
           if (n%i==0) 
           { 
               if (n/i == i) // check if divisors are equal 
                  v.add(i);
               else
               { 
                   v.add(i);
                   // push the second divisor in the vector 
                   v.add(n/i); 
               } 
           } 
       } 
      
       // The vector will be printed in reverse 
      int count=0;
     for (int i=v.size()-1; i>=0; i--) 
           if(isPrime(v.get(i)))
        	   count++;
     return count;
       
   } 
   static boolean isPrime(int n) 
   { 
       // Corner cases 
       if (n <= 1) return false; 
       if (n <= 3) return true; 
     
       // This is checked so that we can skip  
       // middle five numbers in below loop 
       if (n % 2 == 0 || n % 3 == 0) return false; 
     
       for (int i = 5; i * i <= n; i = i + 6) 
           if (n % i == 0 || n % (i + 2) == 0) 
           return false; 
     
       return true; 
   } 
   
   
   
   
}

package codechef;
import java.util.*;
public class XOR
{
	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		int t=sc.nextInt();
		for(int i=0;i<t;i++)
		{
		    int n=sc.nextInt();
		    int arr[]=new int[n];
		    for(int j=0;j<n;j++)
		    {
		        arr[j]=sc.nextInt();
		    
		    }
		    int has[]=new int [100000];
		    int j=0,k;
		    for(j=0;j<n;j++)
		    {
		    	has[arr[j]]++;
		    }
		    
		    
		    int count=0,sum=0;
		    
		    for(j=0;j<n-1;j++)
		    {
		    	sum=arr[j];
		        for(k=i+1;k<n;k++)
		        {
		           sum=sum^arr[k];
		           System.out.println(sum);
		          if( has[sum]==1)
		        	  count++;
				    
		        }
		    }
		    
		    
		   /*
		   while(j<=n-2)
		   {
			   int sum=0,sum1=0;
			   for(int m=j+1;m<=k;m++)
			   {
				   sum=sum^arr[m];
			   }
			   for(int m=0;m<=j;m++)
			   {
				   sum1=sum1^arr[m];
			   }
			  System.out.println(sum1+" "+sum);
			   if(sum==sum1  )
				   count++;
			   else
			   {}
			   if(j<=k-2)
			   {
				   k--;
			   }
			   else
			   {
				   j++;
				   k=n-1;
			   }
			   */
			   
			   
		   
		    System.out.println(count);
		    
		}
	}
}

package codechef;
import java.util.*;
public class Queens_Attack {
 public static void main(String[] args) {
	Scanner sc=new Scanner(System.in);
	int n=sc.nextInt(),k=sc.nextInt();
	int rq=sc.nextInt(),cq=sc.nextInt();
	int arr[][]=new int[n][n];
	for(int i=0;i<k;i++)
	{
	   arr[sc.nextInt()-1][sc.nextInt()-1]=1;
	}
	int count=0,i,j;
	for(i=rq-1;i<n && arr[i][cq]==0;i++);
	{
		if(arr[i][cq]==0)
			count++;
		
	}
	for(i=rq-1;i>=0 && arr[i][cq]==0;i--);
	{
		if(arr[i][cq]==0)
			count++;
		
	}
	for(i=cq-1;i>=0 && arr[rq][i]==0;i--);
	{
		if(arr[rq][i]==0)
			count++;
		
	}
	for(i=cq-1;i<n && arr[rq][i]==0;i--);
	{
		if(arr[rq][i]==0)
			count++;
		
	}
	for( i=rq-1, j=cq-1;i<n && j<n && arr[i][j]==0;i++,j++)
	{
		if(arr[i][j]==0)
			count++;
	}
	for( i=rq-1, j=cq-1;i>=0 && j>=0 && arr[i][j]==0;i--,j--)
	{
		if(arr[i][j]==0)
			count++;
	}
	for( i=rq-1, j=cq-1;i<n && j>=0 && arr[i][j]==0;i++,j--)
	{
		if(arr[i][j]==0)
			count++;
	}
	for( i=rq-1, j=cq-1;i>=0 && j<n && arr[i][j]==0;i--,j++)
	{
		if(arr[i][j]==0)
			count++;
	}
	
	System.out.println(count);
	
	
}
 
 
}

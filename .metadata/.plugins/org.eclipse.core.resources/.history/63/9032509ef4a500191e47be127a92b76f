import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;
import java.util.Scanner;

public class Circular {
 public static void main(String[] args) {
	
	Scanner sc=new Scanner(System.in);
	int t;
	t=sc.nextInt();
	ArrayList<Integer> arr=new ArrayList<Integer>();
	for(int i=0;i<t;i++)
	{
		int n=sc.nextInt();
		int sum=0,penal=0;
		for(int j=0;j<n;j++)
		{
			int m=sc.nextInt();
		   arr.add(m);
		}
		
		
		while(arr.size()>2)
		{
		
		
		int a,b;	
		int small=smallest(arr);	
		int id=arr.indexOf(small); 
		System.out.println(arr);
		a=arr.get(id);
		if(id==(arr.size()-1))
		{
			 if(arr.get(0)<arr.get(id-1))
			 {
				 b=arr.get(0);
			 }
			 else
				 b=arr.get(id-1);
		}
		else if(id==0)
		{
			if(arr.get(arr.size()-1)<arr.get(id+1))
			 {
				 b=arr.get(arr.size()-1);
			 }
			 else
				 b=arr.get(id+1);
		}
		else
		{
			if(arr.get(id+1)<arr.get(id-1))
			 {
				 b=arr.get(id+1);
			 }
			 else
				 b=arr.get(id-1);
		}
			
		
		 sum=a+b;
		// System.out.println(sum);
		// System.out.println(a);
		// System.out.println(b);
		 penal=penal+sum;
		 
		// arr.add(0,sum);
		
		 arr.remove(id);
		 arr.set(id,sum);
		// arr.stream().mapToInt(Integer::intValue).toArray(); 
		 
		 //System.out.println(arr.size());
		}
		//penal=penal+arr.get(0)+arr.get(1);
		System.out.println(penal);
		
	}
	 
 }

private static int smallest(ArrayList<Integer> arr) {
	// TODO Auto-generated method stub
	  //Collections.sort(arr1);;
	  int a[]=arr.toArray(new int[arr.size()]);  
	
	  
	return small;
	}
 
}

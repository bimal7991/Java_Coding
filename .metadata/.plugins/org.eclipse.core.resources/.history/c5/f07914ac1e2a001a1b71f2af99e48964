package codechef;
import java.util.*;
public class Stupid {
   public static void main(String[] args) {
	Scanner sc=new Scanner(System.in);
	int T=sc.nextInt();
	for(int t=0;t<T;t++)
	{
		int n=sc.nextInt();
		int arr[]=new int[n];
		int min=Integer.MAX_VALUE,min_ind=0;
		long max=0;
		for(int i=0;i<n;i++)
		{
			arr[i]=sc.nextInt();
			if(arr[i]<min)
			{
				min=arr[i];
				min_ind=i;
			}			
		}
		for(int i=0;i<=min_ind;i++)
		   {
			 arr[i]=arr[i]-min;
			 
		   }
		max=min*n;
		
		while(arr[0]>0)
		{
		  min=Integer.MAX_VALUE;	
		  for(int i=0;i<min_ind;i++)
		   {
			  if(arr[i]<min)
				{
					min=arr[i];
					min_ind=i;
				}
		   }
		  for(int i=0;i<=min_ind;i++)
		   {
			 arr[i]=arr[i]-min;
			 
		   }
		  max=max+(long)min*(min_ind+1);
		}
       System.out.println(max);
	}
}
}

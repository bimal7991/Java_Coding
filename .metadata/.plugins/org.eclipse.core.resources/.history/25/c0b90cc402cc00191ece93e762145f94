package codechef;
import java.util.*;
public class Gekks {
	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		int t=sc.nextInt();

		for(int T=0;T<t;T++)
		{
			int a[]=new int[3];
			int b[]=new int[3];
			int temp1[] = new int[10];
					for(int i=0;i<3;i++)
					{
						a[i]=sc.nextInt();	
					}
			for(int i=0;i<3;i++)
			{
				b[i]=sc.nextInt();	
			}
			sort3(a,b,temp1);
			int flag=0;
            for(int i=0;i<2;i++)
            {
            	if(b[i]>b[i+1])
            		flag=1;
            	
            }
          if(flag==1)
        	  System.out.println("Fair");
          else
        	  System.out.println("Not Fair");


		}
	}
	
	static void sort3(int arr[],int b[] ,int temp[]) 
	{ 
		// Insert arr[1] 
		if (arr[1] < arr[0]) 
		{ 
			temp[0] = arr[0]; 
			arr[0] = arr[1]; 
			arr[1] = temp[0]; 

			temp[0]=b[0];
			b[0]=b[1];
			b[1]=temp[0];

		} 

		// Insert arr[2] 
		if (arr[2] < arr[1]) 
		{ 
			temp[0] = arr[1]; 
			arr[1] = arr[2]; 
			arr[2] = temp[0]; 

			temp[0]=b[1];
			b[1]=b[2];
			b[2]=temp[0];

			if (arr[1] < arr[0]) 
			{ 
				temp[0] = arr[0]; 
				arr[0] = arr[1]; 
				arr[1] = temp[0]; 
				temp[0]=b[0];
				b[0]=b[1];
				b[1]=temp[0];
			} 
		}
	}
}

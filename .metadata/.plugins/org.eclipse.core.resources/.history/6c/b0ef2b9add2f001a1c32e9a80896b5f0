package codechef;
import java.util.*;

public class ISBIAS {
  public static void main(String[] args) {
	Scanner sc=new Scanner(System.in);
	int N=sc.nextInt(),Q=sc.nextInt();
	int arr[]=new int[N+1];
	for(int i=1;i<=N;i++)
	{
		arr[i]=sc.nextInt();
		
	}
	ArrayList<Integer> arr1=new ArrayList<Integer>();
	ArrayList<Integer> arr3=new ArrayList<Integer>();
	ArrayList<Integer> arr2=new ArrayList<Integer>();
	ArrayList<Integer> arr4=new ArrayList<Integer>();
	int no=0;
	
	for(int i=1;i<N;i++)
	{
		if(arr[i]<arr[i+1])
    	{
			if(no==0)
				arr3.add(i);
    		no++;
    	    if(i+1==N)
    	    	arr1.add(i+1);
    	}
    	else
    	{
    		if(no>=1)
    		{
    			arr1.add(i);	
    		    no=0;
    		}
    	}
		
	}
	no=0;
	for(int i=1;i<N;i++)
	{
		if(arr[i]>arr[i+1])
    	{
			if(no==0)
				arr4.add(i);
    		no++;
    	    if(i+1==N)
    	    	arr2.add(i+1);
    	}
    	else
    	{
    		if(no>=1)
    		{
    			arr2.add(i);	
    		    no=0;
    		}
    	}
		
	}
	//System.out.println(arr1);
   // System.out.println(arr2);
   // System.out.println(arr3);
    //System.out.println(arr4);
	/*
	for(int q=0;q<Q;q++)
	{
		int R=sc.nextInt(),L=sc.nextInt();
		int count1=0,no=0;
	    for(int i=R;i<=L-1;i++)
	    {
	    	if(arr[i]<arr[i+1])
	    	{
	    		no++;
	    	    if(i+1==L)
	    	    	count1++;
	    	}
	    	else
	    	{
	    		if(no>=1)
	    		{
	    		count1++;
	    		no=0;
	    		}
	    	}
	    		
	    }
	    int count2=0,no1=0;
	    for(int i=R;i<=L-1;i++)
	    {
	    	if(arr[i]>arr[i+1])
	    	{
	    		no1++;
	    		if(i+1==L)
	    	    	count2++;
	    	}
	    	else
	    	{
	    		if(no1>=1)
	    		{
	    		 count2++;
	    		 no1=0;
	    		}
	    	}
	    		arr1.add(i+1);
	    }*/
	 for(int q=0;q<Q;q++)
	 {
		int L=sc.nextInt(),R=sc.nextInt(); 
		if(L==1 && R==N)
		{
			if(arr1.size()==arr2.size())
				System.out.println("YES");
			else
				System.out.println("NO");
			continue;
		}
		
	    int count1=0,count2=0;
	    for(int i=0;i<arr1.size();i++)
	    {
	    	if(arr1.get(i)<=R && arr1.get(i)>L)
	    		count1++;
	    	else if(arr1.get(i)>R && arr3.get(i)<R)
	    		count1++;
	    	else
	    	{}
	    }
	    
	    for(int i=0;i<arr2.size();i++)
	    {
	    	if(arr2.get(i)<=R && arr2.get(i)>L)
	    		count2++;
	    	else if(arr2.get(i)>R && arr4.get(i)<R)
	    		count2++;
	    	else
	    	{}
	    }
	
	
	   //System.out.println(count1+" "+count2);
	   if(count1==count2)
		   System.out.println("YES");
	   else
		   System.out.println("NO"); 
	}
}
}
